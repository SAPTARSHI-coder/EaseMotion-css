name: Performance Benchmark

on:
  pull_request:
  push:

jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run Benchmark
        run: node benchmarks/intersectionobserver-benchmark.js