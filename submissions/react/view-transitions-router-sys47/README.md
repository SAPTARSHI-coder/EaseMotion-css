# React View Transitions API Declarative Router Animator

## Overview
React router animator hooking into browser-native View Transitions API (`document.startViewTransition`) for seamless page reveals.

## Structure
- `demo.html` — View transition route showcase
- `style.css` — `::view-transition` CSS rules
- `README.md` — Documentation
