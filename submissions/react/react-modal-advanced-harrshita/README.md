# EaseModal React Component

React modal with backdrop blur and escape key listener.

## Usage
```jsx
import EaseModal from './EaseModal';
<EaseModal isOpen={isOpen} onClose={() => setIsOpen(false)} title="My Modal">
  <p>Modal content</p>
</EaseModal>
```
