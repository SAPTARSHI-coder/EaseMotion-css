# Lightning Flash Burst Spark

## What does this do?

Adds a reusable CSS animation that creates a short lightning-style flash, burst, and fade effect using `transform` and `opacity`.

## How is it used?

Add the animation class to an element:

```html
<div class="ease-anim-lightning-flash-burst-spark">
  ⚡
</div>