# Fix: Tab Keyboard Arrow Navigation

Resolves keyboard navigation limits on standard tab list components.

## What does this do?
- **Arrow Handlers:** Adds keydown listeners to switch tab selections in response to arrow triggers.