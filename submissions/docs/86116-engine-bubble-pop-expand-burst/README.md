# Bubble Pop Expand Burst

A reusable EaseMotion core animation utility that creates a playful
bubble-style entrance effect.

The element starts small and slightly offset, quickly expands beyond its
final size, performs a subtle overshoot sequence, and settles smoothly
into its normal scale.

## Animation Name

```css
ease-bubble-pop-expand-burst