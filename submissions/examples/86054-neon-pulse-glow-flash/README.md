# Neon Pulse Glow Flash

A reusable CSS keyframe animation utility for creating a smooth
neon pulse and glow flash effect.

## Animation

The utility defines:

```css
@keyframes ease-neon-pulse-glow-flash