# Fitness Workout Card

Closes #74871

Workout progress card with an SVG radial progress ring, gradient stroke, and stats grid.

## Technique

SVG circle with `stroke-dasharray` and `stroke-dashoffset` controls the fill amount.
Linear gradient applied to the stroke for a vibrant color blend.
