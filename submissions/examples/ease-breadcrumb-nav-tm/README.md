# Breadcrumb Navigation Component

Add ease-breadcrumb-nav-tm pure CSS component.

## Design Tokens

| Token | Default |
|-------|---------|
| `--brn-accent` | `#6366f1` |
| `--brn-radius` | `8px` |

## Technique

Semantic `<ol>` list with flexbox separators. Hover highlights individual links.
