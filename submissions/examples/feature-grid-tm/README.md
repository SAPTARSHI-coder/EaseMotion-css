# Staggered feature grid

1. What does this do? A grid of feature cards whose items fade up in sequence on first paint.

2. How is it used? Add `feature-grid-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Marketing / product-overview pattern; stagger uses incremental animation-delay.
