# Container Fluid

## What does this do?
Container Fluid component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="cnl-primary">Container Fluid Primary</div>
```

## Why is it useful?
Reusable container fluid UI element. Dark theme with CSS custom properties.
