# Orbit spinner

1. What does this do? A small dot that orbits a circular track.

2. How is it used? Add `spinner-orbit-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Loading pattern; the orbit uses rotate on a wrapper.
