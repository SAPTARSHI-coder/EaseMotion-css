# ease-ripple-click

A CSS-only ripple click effect component for the EaseMotion CSS library.

## Features
- Pure CSS, no JavaScript
- Respects `prefers-reduced-motion`
- Responsive design

## Usage
Include `style.css` and use the HTML from `demo.html`.

## Accessibility
Animations are disabled when users prefer reduced motion.