# Corner Draw Border

Pure CSS corner-drawing border animation.

```scss
@mixin corner-draw-border($duration:1.5s,$timing:ease-in-out){
  animation:ease-corner-draw-border $duration $timing infinite;
}