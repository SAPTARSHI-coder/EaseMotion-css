# Spiral Spin Rotate

CSS animation with spiral-style movement, rotation and scaling.

## Utility

```css
.ease-anim-spiral-spin-rotate{
  animation:ease-spiral-spin-rotate
    var(--ease-duration,2s)
    var(--ease-timing,ease-in-out)
    both;
}