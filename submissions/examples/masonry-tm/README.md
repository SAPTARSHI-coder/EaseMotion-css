# Masonry grid

1. What does this do? A masonry-style grid where items of varying height sit in columns.

2. How is it used? Add `masonry-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Gallery and blog-index pattern; uses CSS columns for layout.
