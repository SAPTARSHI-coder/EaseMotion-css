# Deadlock Wait-For Graph

Deadlock Wait-For Graph is an advanced CSS animation that visualizes database transactions waiting on each other until the detector isolates the cycle and selects a rollback victim.

## Features

- Animated wait-for dependency graph with cycle tracing
- Transaction nodes with pulsing lock states
- Rollback victim selection highlight
- Responsive dashboard layout
- Accessible semantic demo markup
