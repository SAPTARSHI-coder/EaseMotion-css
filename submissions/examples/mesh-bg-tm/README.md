# Mesh gradient background

1. What does this do? A background that blends three soft radial gradients like a mesh.

2. How is it used? Add `mesh-bg-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Hero pattern; the mesh feel is the modern default for product hero sections.
