# Sticky Header Component

Add ease-header-sticky-tm pure CSS component.

## Design Tokens

| Token | Default |
|-------|---------|
| `--sth-accent` | `#6366f1` |
| `--sth-h` | `60px` |

## Technique

`position: sticky; top: 0` keeps header fixed within its scroll container. Pure CSS.
