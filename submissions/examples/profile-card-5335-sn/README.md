# Profile Card Component With Avatar, Name, And Actions

A pure CSS implementation for profile card component with avatar, name, and actions.

## Usage
Include `style.css` and use the provided classes.

## Features
- CSS only
- Uses EaseMotion variables
- Responsive
