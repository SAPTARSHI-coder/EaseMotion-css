# ease-hover-button-arrow-slide-sap

A CTA button whose trailing arrow nudges to the right on hover, a common lightweight "call to action" affordance.

## Notes
- A tiny, cheap effect — just one transform on one element — that's deliberately subtle rather than showy.
- Respects `prefers-reduced-motion`: arrow nudge transition removed.