# Pure CSS Animated Chat Bubble

A classic "typing indicator" chat bubble that communicates activity.

## Details
- Features three dots bouncing via `transform: translateY` inside `@keyframes`.
- Relies on specific negative `animation-delay` values to desync the bouncing without JavaScript logic.
- Rounded corners optimized to look like a native messaging application.
