# ease-scroll-nav-active-underline-sap

A sticky top nav where the underline moves to whichever section link corresponds to the currently-visible page section.

## Notes
- Similar tracking pattern to the sticky TOC sidebar, applied to a horizontal top nav with a `scaleX()` underline instead of a border-left indicator.
- Respects `prefers-reduced-motion`: underline scale transition removed.