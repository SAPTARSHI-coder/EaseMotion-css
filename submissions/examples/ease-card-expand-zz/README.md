# Card Expand

## What does this do?
Card Expand component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="cce-primary">Card Expand Primary</div>
```

## Why is it useful?
Reusable card expand UI element. Dark theme with CSS custom properties.
