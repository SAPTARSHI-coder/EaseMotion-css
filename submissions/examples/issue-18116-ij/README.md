# ease-cursor-follower

A custom cursor system with an instant-follow dot and a smooth trailing ring element.

**Usage:** Include `style.css` and the inline script from `demo.html`. The dot (`#cursorDot`) follows the cursor instantly. The ring (`#cursorRing`) trails with a lerp factor of 0.15 for smooth motion.
