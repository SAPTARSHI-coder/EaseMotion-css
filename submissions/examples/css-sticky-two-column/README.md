# CSS Sticky Two Column

A responsive two-column layout with a sticky left column and scrolling right-side content.

## Features

- Pure CSS implementation
- Sticky left column
- Responsive two-column layout
- Automatically stacks on smaller screens
- Uses CSS Grid and `position: sticky`
- No JavaScript required

## Usage

Include the stylesheet:

```html
<link rel="stylesheet" href="style.css">