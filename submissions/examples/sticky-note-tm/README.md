# Sticky note

1. What does this do? A small yellow note with a soft shadow and slight rotation.

2. How is it used? Add `sticky-note-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Note / annotation pattern; the rotation is per-instance via custom property.
