# Button Dashed

## What does this do?
Button Dashed component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="btd-primary">Button Dashed Primary</div>
```

## Why is it useful?
Reusable button dashed UI element. Dark theme with CSS custom properties.
