# Removable tag pill

1. What does this do? A small pill-shaped tag with an inline close button.

2. How is it used? Add `tag-pill-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Filter, chip, and input-tag pattern; the close button fades in on hover.
