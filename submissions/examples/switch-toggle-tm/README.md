# Toggle switch

1. What does this do? A sliding switch with a knob that travels on a track; reflects checked state.

2. How is it used? Add `switch-toggle-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Settings-row pattern; CSS-only toggle that follows :checked state.
