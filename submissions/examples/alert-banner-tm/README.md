# Alert banner

1. What does this do? A full-width banner at the top of a page that announces a system state.

2. How is it used? Add `alert-banner-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Status / announcement pattern; the banner has a left accent bar.
