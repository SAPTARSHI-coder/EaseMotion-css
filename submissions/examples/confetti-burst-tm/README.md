# Confetti burst

1. What does this do? On trigger, a burst of small coloured squares falls and fades.

2. How is it used? Add `confetti-burst-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Success state / celebration pattern; the burst is CSS-only via keyframes.
