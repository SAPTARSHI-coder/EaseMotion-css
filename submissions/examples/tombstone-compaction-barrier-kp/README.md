# Tombstone Compaction Barrier

Tombstone Compaction Barrier is an advanced CSS animation that visualizes delete markers, replica lag, and a guarded compaction window. It shows how storage engines wait for safe visibility before reclaiming old records.

## Features

- Animated tombstone lanes and compaction gate
- Replica lag pulses and reclaim sweep
- Responsive layout with reduced-motion support
- Semantic accessible demo markup