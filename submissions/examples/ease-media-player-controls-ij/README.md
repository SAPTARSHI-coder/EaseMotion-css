# ease-media-player-controls

A CSS-only media player controls component for the EaseMotion CSS library.

## Features
- Pure CSS with no JavaScript dependencies
- Respects `prefers-reduced-motion` media query
- Responsive design

## Usage
Include `style.css` and use the HTML structure from `demo.html`.

## Accessibility
Animations are disabled when users prefer reduced motion.