# Breadcrumb Arrow

## What does this do?
Breadcrumb Arrow component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bca-primary">Breadcrumb Arrow Primary</div>
```

## Why is it useful?
Reusable breadcrumb arrow UI element. Dark theme with CSS custom properties.
