# ease-shimmer-waves

An animated gradient text effect where a linear gradient sweeps horizontally to create a shimmering wave.

**Usage:** Include `style.css` only. Apply the `.shimmer` class to any text element. The gradient background shifts from right to left in a 3-second loop. Customize colors in the `background` property.
