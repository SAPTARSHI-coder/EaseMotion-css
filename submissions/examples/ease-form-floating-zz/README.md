# Form Floating

## What does this do?
Form Floating component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="fmf-primary">Form Floating Primary</div>
```

## Why is it useful?
Reusable form floating UI element. Dark theme with CSS custom properties.
