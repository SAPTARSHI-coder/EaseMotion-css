# Dot grid pattern

1. What does this do? A repeating dot grid used as a section background.

2. How is it used? Add `dot-grid-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Section / hero pattern; pair with a fade-out mask to soften the edge.
