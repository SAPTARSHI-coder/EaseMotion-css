# Parallax Depth

## What does this do?
Adds `.ease-parallax` — a container with parallax depth layers on scroll.

```html
<div class="ease-parallax">
  <div class="ease-parallax-layer">Back</div>
</div>
```

## Preview
Open `demo.html` in your browser.
