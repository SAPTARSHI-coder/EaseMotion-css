# Avatar XS

## What does this do?
Avatar XS component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="avx-primary">Avatar XS Primary</div>
```

## Why is it useful?
Reusable avatar xs UI element. Dark theme with CSS custom properties.
