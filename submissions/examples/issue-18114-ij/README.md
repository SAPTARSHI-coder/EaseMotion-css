# ease-parallax-scroll

Creates a parallax scrolling effect where background, midground, and foreground layers move at different speeds.

**Usage:** Include `style.css` and the inline script from `demo.html`. The `.parallax-stage` container is scrollable. Layer speeds are controlled by multipliers (0.3, 0.6, 0.9) in the scroll event handler.
