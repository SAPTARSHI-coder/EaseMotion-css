# elastic-spring-bounce-badge

A badge element scaling dynamically with bouncy spring transformations.

## Features
- Pure CSS layout logic.
- Light and dark theme adaptable.
- Clean semantic HTML structure.
