# Form Stacked

## What does this do?
Form Stacked component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="fms-primary">Form Stacked Primary</div>
```

## Why is it useful?
Reusable form stacked UI element. Dark theme with CSS custom properties.
