# Induction Coil Arc

## What does this do?
Renders a self-contained CSS-only `ease-induction-coil-arc` demo: Induction coil secondary arc snapping across electrodes.

## How is it used?
Open `demo.html` in a browser. Motion uses classes under `ease-induction-coil-arc`. No build step or JavaScript is required for the effect.

```html
<section class="ease-induction-coil-arc">
  <div class="ease-induction-coil-arc__housing">...</div>
</section>
```

## Why is it useful?
It packs a recognizable real-world motion metaphor into three submission files, fitting EaseMotion's curated CSS-first model and giving maintainers a concrete effect to standardize into `ease-*` utilities.
