# Segmented control

1. What does this do? An iOS-style segmented control with a sliding selection indicator.

2. How is it used? Add `nav-segmented-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Nav / filter pattern; the indicator animates between segments.
