# ease-neon-button

A CSS-only neon button component.

## Features
- Pure CSS, no JavaScript
- Respects `prefers-reduced-motion`
- Responsive design

## Usage
Include `style.css` and use HTML from `demo.html`.