# Swing Pendulum Wobble

Pure CSS pendulum-style wobble animation.

```scss
@mixin swing-pendulum-wobble($duration:2s,$timing:ease-in-out){
  animation:ease-swing-pendulum-wobble $duration $timing infinite;
}