# Badge Pill

## What does this do?
Badge Pill component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="bpl-primary">Badge Pill Primary</div>
```

## Why is it useful?
Reusable badge pill UI element. Dark theme with CSS custom properties.
