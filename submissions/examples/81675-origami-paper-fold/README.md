# Origami Paper Fold

CSS animation demonstrating an origami-style paper fold.

## Utility

```css
.ease-anim-origami-paper-fold{
  animation:ease-origami-paper-fold var(--ease-duration,1s) var(--ease-timing,ease) both;
}