# Ring spinner

1. What does this do? A circular spinner that rotates continuously.

2. How is it used? Add `spinner-ring-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Standard loading pattern; uses border + border-top-color.
