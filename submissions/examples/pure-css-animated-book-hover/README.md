# Pure CSS Animated Book Hover

A realistic 3D book that opens its cover and pages on hover.

## Technical Details
- Uses `transform-origin: left` to ensure the pages hinge at the spine.
- `rotateY` combined with `transition-delay` creates a cascading page-open animation.
- A beautiful skeuomorphic design for portfolios or documentation blocks.
