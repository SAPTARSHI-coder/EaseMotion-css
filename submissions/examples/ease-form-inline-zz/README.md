# Form Inline

## What does this do?
Form Inline component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="fmi-primary">Form Inline Primary</div>
```

## Why is it useful?
Reusable form inline UI element. Dark theme with CSS custom properties.
