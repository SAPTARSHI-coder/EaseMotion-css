# CSS-only 3D Dice Roller

A fully CSS 3D cube featuring continuous rotation using keyframes.

## Technical Notes
- Uses `transform-style: preserve-3d` to render the faces in a 3D context.
- Each face is positioned using `rotateX/Y` and `translateZ`.
- Infinite rolling animation.
