# CSS-only Expanding Search Bar

A minimalist search interaction that saves screen real estate.

## Details
- Starts as a circular icon-like input.
- Uses `:focus` pseudo-class to trigger width expansion.
- The sibling combinator (`~`) is used to change the color of the search icon when the input is focused.
