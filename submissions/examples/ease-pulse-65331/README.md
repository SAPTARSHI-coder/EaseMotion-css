@'
# Ease Pulse Animation Utility

## Overview

`ease-pulse` is a subtle repeating animation utility for drawing attention to notifications, status indicators, and other important interface elements.

## Usage

Add the `ease-pulse` class to any element that should receive the pulse effect:

```html
<div class="ease-pulse">
  New Notification
</div>