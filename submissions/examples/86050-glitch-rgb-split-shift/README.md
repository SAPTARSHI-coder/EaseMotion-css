# Glitch RGB Split Shift

A lightweight CSS motion utility that creates a digital glitch effect by shifting
RGB-style visual channels independently.

## Animation

```css
@keyframes ease-glitch-rgb-split-shift