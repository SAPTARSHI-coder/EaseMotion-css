# radar-sweep-xy
A sonar display UI where a gradient beam sweeps continuously over target blips and a status tag blinks.

## Run
Open `demo.html` directly in a browser to view the radar sweep effect.

## Notes
- The sweep hand rotates 360 degrees using conic gradients.
- Target blips pulse in sequence with timing delays.
- Zero dependencies and pure CSS keyframes.