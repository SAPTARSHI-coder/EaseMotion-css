# ease-password-mismatch-shake

A shake animation triggered when password confirmation does not match.

## Usage
Open demo.html. Enter two different passwords and click Submit to trigger the shake animation.

## Custom Properties
| Property | Default | Description |
|----------|---------|-------------|
| --primary | #ef4444 | Error color |

## Notes
CSS keyframe animation handles the shake. JS compares password values.
