# Checkbox Inline

## What does this do?
Checkbox Inline component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="chi-primary">Checkbox Inline Primary</div>
```

## Why is it useful?
Reusable checkbox inline UI element. Dark theme with CSS custom properties.
