# Holographic Sheen Beam

A reusable CSS keyframe animation utility that creates a smooth
holographic light beam sweeping across an element.

## Animation

The utility defines:

```css
@keyframes ease-holographic-sheen-beam