# Particle Float Drift

Pure CSS floating particle animation.

## Utility

```scss
@mixin particle-float-drift($duration:4s,$timing:ease-in-out){
  animation:ease-particle-float-drift $duration $timing infinite alternate;
}