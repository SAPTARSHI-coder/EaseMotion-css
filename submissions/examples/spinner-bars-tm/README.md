# Bars spinner

1. What does this do? Four vertical bars whose heights animate in a wave pattern.

2. How is it used? Add `spinner-bars-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Loading pattern; bars use scaleY with staggered delays.
