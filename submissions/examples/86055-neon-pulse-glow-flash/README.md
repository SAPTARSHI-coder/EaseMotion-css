# Neon Pulse Glow Flash

A reusable CSS animation utility that creates a smooth neon pulse and
glow flash effect.

## Animation

The animation is defined as:

```css
@keyframes ease-neon-pulse-glow-flash