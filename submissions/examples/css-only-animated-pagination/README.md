# CSS-only Animated Pagination

A beautiful dot pagination UI with stretching animations.

## How it works
- Utilizes the hidden radio button hack to maintain state without JavaScript.
- The active dot morphs from a circle into a rounded pill shape by transitioning `width` and `border-radius`.
- Clean spring-like bounce added using `cubic-bezier`.
