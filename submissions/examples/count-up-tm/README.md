# Count-up number

1. What does this do? A number that animates from 0 to a target using @property interpolation.

2. How is it used? Add `count-up-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Dashboard / hero stat pattern; CSS-only via @property + counter-reset.
