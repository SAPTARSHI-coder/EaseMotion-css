# CSS Random Number Animation

A CSS-only animation demonstrating the proposed `random()` function from
CSS Values and Units Level 5.

## What it does

The demo uses CSS `random()` values to vary:

- Orb positions
- Animation durations
- Motion direction
- Visual movement

No JavaScript is required.

## Usage

Link the stylesheet:

```html
<link rel="stylesheet" href="style.css">