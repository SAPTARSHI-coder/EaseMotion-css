# Particle Float Drift Rise

A lightweight CSS animation utility that makes elements gently float,
drift horizontally, rise upward, and fade out.

## Animation

The utility provides the following keyframe animation:

```css
@keyframes ease-particle-float-drift-rise