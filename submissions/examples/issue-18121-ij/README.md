# ease-morph-shape

A CSS-only shape morphing animation using `border-radius` keyframes to transition through organic blob-like forms.

**Usage:** Include `style.css` only. The `.morph-box` element cycles through 5 border-radius configurations over 4 seconds. Modify the `@keyframes morph-shape` to change the morph sequence.
