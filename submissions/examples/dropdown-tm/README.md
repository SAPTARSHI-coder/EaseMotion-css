# Dropdown menu

1. What does this do? A menu that drops down from a button with a fade and slight slide.

2. How is it used? Add `dropdown-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Standard menu pattern; the focus-visible support keeps it accessible.
