# CSS-only Animated Battery Indicator

A realistic battery charging animation.

## Technical Details
- Uses CSS `@keyframes` to simultaneously animate `width` and `background-color`.
- A pseudo-element `::after` creates the battery terminal.
- Perfectly clean scaling.
