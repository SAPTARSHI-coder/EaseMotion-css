# Water Drop Ripple Expand

A water-drop-inspired ripple expansion animation preset for EaseMotion CSS.

## Animation

The `ease-anim-water-drop-ripple-expand` class creates a smooth expanding ripple effect using GPU-friendly `transform` and `opacity` properties.

## Usage

```html
<div class="ease-anim-water-drop-ripple-expand"></div>