# Animated checkbox

1. What does this do? A checkbox whose checkmark draws itself in on toggle.

2. How is it used? Add `checkbox-anim-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Forms pattern; the check is drawn as a CSS border on a rotated pseudo-element.
