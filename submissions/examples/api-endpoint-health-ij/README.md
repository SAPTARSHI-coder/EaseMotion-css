# ease-api-endpoint-health

An API health panel where the dots ping, the latency flashes, and the rows slide in.

## Run
Open `demo.html` directly in a browser to watch the panel.

## Notes
- The status dots ping on each endpoint.
- The latency values flash on the rows.
- The endpoint rows slide in turn.