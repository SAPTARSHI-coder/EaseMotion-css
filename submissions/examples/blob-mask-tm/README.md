# Animated blob mask

1. What does this do? An element masked by an SVG-free blob shape that slowly morphs.

2. How is it used? Add `blob-mask-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Hero / empty-state pattern; the morphing mask is organic without any JS.
