# Glitch RGB Split Shift - SCSS Animation Mixin

A reusable SCSS animation mixin pattern for creating a digital RGB glitch
split-and-shift effect.

## Keyframe

```scss
@keyframes ease-glitch-rgb-split-shift