# CSS Rotate-Fade Pricing Table

A modern **CSS Rotate-Fade Pricing Table** built entirely with **HTML5** and **CSS3**. This showcase demonstrates a responsive pricing table with smooth rotate-and-fade hover animations, minimalist tech styling, and accessible interactions—without requiring JavaScript.

---

## ✨ Features

- 🚀 Pure HTML5 & CSS3
- 🔄 Rotate-fade hover animation
- 💳 Three responsive pricing plans
- ⭐ Highlighted featured plan
- 📱 Fully responsive layout
- ♿ Keyboard-accessible buttons with visible focus styles
- 🎨 Easy customization using CSS Custom Properties
- 🌙 Supports `prefers-reduced-motion`
- ⚙️ No JavaScript or external dependencies

---

## 📂 Folder Structure

```text
minimalist-rotate-fade-pricing-table/
├── demo.html
├── style.css
└── README.md
```

---

## 🚀 Usage

1. Open `demo.html` in any modern web browser.
2. Hover over a pricing card to view the rotate-fade animation.
3. Use the call-to-action buttons to preview interactive states.
4. Customize colors, spacing, and typography through the CSS custom properties.

---

## 🎨 CSS Custom Properties

| Variable | Description |
|----------|-------------|
| `--bg` | Page background color |
| `--surface` | Card background |
| `--primary` | Primary accent color |
| `--secondary` | Secondary accent color |
| `--text` | Primary text color |
| `--muted` | Secondary text color |
| `--border` | Card border color |
| `--radius` | Card border radius |
| `--shadow` | Card shadow |
| `--transition` | Transition timing |

Example:

```css
:root{
    --primary:#2563eb;
    --secondary:#38bdf8;
}
```

---

## 📱 Responsive Design

Optimized for:

- 💻 Desktop (3-column layout)
- 💼 Laptop
- 📱 Tablet
- 📱 Mobile (single-column layout)

The pricing cards automatically stack on smaller screens to improve readability and usability.

---

## ♿ Accessibility

This example includes:

- Semantic HTML (`main`, `header`, `section`, `article`)
- Keyboard-accessible buttons
- Visible keyboard focus styles
- `prefers-reduced-motion` support

---

## 🌐 Browser Support

- Google Chrome
- Microsoft Edge
- Mozilla Firefox
- Safari

---

## 📄 License

This example follows the same license as the EaseMotion CSS project.