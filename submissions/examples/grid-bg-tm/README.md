# Animated grid background

1. What does this do? A grid of dots that subtly shift position to give the page a sense of life.

2. How is it used? Add `grid-bg-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Hero / dashboard pattern; the grid is alive but stays in the background.
