# CSS Autocomplete

Native autocomplete using `<datalist>` with styled input.

## Usage
```html
<div class="autocomplete">
  <input list="options">
  <datalist id="options"><option value="Item"></datalist>
</div>
```

## Why EaseMotion CSS?
Shows how to style the native datalist autocomplete pattern.
