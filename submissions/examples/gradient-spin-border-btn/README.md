# Gradient Spin Border Button

1. What does this do? Rotates a conic gradient around a button's border.
2. How is it used? Apply the class `.gradient-spin-btn` to a button element.
3. Why is it useful? It creates an eye-catching animated border with only CSS.
