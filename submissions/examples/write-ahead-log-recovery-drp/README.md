# Write-Ahead Log Recovery

Write-Ahead Log Recovery is an advanced CSS animation that a pure CSS recovery sequence where log records flush before data pages, then redo and undo phases rebuild a safe database state.

## Features

- Animated WAL flush ordering
- Redo and undo recovery lanes
- Checkpoint and dirty-page markers
- Responsive storage recovery panel
- Accessible semantic demo markup
