# CSS Floating Label Select

A pure CSS select dropdown with a floating label effect, similar to modern text input fields.

## Features

- Pure CSS implementation
- Floating label animation
- Responsive design
- Keyboard accessible
- No JavaScript required
- Works with native `<select>` elements

## Usage

Include the stylesheet:

```html
<link rel="stylesheet" href="style.css">