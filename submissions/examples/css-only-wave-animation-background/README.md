# CSS-only Wave Animation Background

A smooth, overlapping wave animation using SVG data URIs inside CSS.

## Architecture
- Layered divs running at different speeds to create a parallax depth effect.
- Zero external image assets; completely self-contained.
- Endless fluid background loop.
