# Card Overlay

## What does this do?
Card Overlay component with primary, secondary, and outline variants. Dark theme with CSS custom properties.

## How is it used?
```html
<div class="cco-primary">Card Overlay Primary</div>
```

## Why is it useful?
Reusable card overlay UI element. Dark theme with CSS custom properties.
