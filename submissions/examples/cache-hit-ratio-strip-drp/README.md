# Cache Hit Ratio Strip

A CSS-only performance strip that compares hit and miss states with a clean segmented ratio display.

## Features

- Segmented ratio bar
- Hit and miss states
- Responsive strip layout
- Accessible semantic markup
