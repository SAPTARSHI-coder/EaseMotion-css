# File tree

1. What does this do? An indented file tree with folder toggles and small icons.

2. How is it used? Add `file-tree-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Docs / explorer pattern; indent uses padding, toggle uses a chevron.
