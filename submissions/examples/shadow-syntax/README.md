# Shadow Syntax

Shadow Syntax is an experimental interactive component where shadows act as a visual language.

Instead of presenting every state directly through conventional UI elements, each object communicates hidden information through the shadow it casts.

The component is built around three elements:

```text
OBJECT
   ↓
LIGHT
   ↓
SHADOW