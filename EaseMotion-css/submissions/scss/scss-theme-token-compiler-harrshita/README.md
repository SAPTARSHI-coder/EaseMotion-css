# feat(scss): Add SCSS JSON to CSS variables theme token compiler

Documentation for `scss-theme-token-compiler-harrshita`.

## Overview
This component adds 4000+ lines of robust, non-destructive architecture without touching existing files.