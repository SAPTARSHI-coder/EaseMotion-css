# Water Drop Ripple Expand

A smooth water-drop-inspired ripple expansion animation for EaseMotion CSS.

## Features

- Smooth ripple expansion effect
- Uses `transform` and `opacity`
- Configurable animation duration
- Configurable timing function
- Supports `prefers-reduced-motion`
- Uses GPU-friendly animation properties

## Usage

```html
<div class="ripple"></div>