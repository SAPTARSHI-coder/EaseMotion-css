# ease-qr-code-scanner-frame

A QR scanner viewfinder where the scan line sweeps, the corner brackets pulse, and the label blinks.

## Run
Open `demo.html` directly in a browser to watch the scanner.

## Notes
- The scan line sweeps across the viewfinder.
- The corner brackets pulse in sequence.
- The align label blinks below the frame.