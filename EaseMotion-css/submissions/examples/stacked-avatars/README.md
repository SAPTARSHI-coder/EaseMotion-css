# Stacked Avatars Animation

A pure CSS interactive avatar stack that spreads open fluidly on hover.

## Features

- **Smooth Overlap Expand**: Uses negative margins and CSS transitions for elastic expansion on hover.
- **Dynamic Elevation**: Hovered avatars dynamically elevate above the stack using `z-index` and scale transforms.
- **Zero JS Dependencies**: Built entirely with HTML and CSS.