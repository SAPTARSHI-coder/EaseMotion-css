# Terminal window

1. What does this do? A faux terminal window with a coloured title bar and a prompt line.

2. How is it used? Add `terminal-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Docs / demo pattern; the title-bar dots are pure CSS.
