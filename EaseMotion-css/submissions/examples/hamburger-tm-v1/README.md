# Animated hamburger

1. What does this do? A three-line icon that morphs into an X when toggled.

2. How is it used? Add `hamburger-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Common mobile menu trigger; morph is smoother than a swap.
