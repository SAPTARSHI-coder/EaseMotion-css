# Phantom Read Predicate Window

Phantom Read Predicate Window is an advanced CSS animation that visualizes range scans, predicate locks, and blocked phantom inserts inside a serializable transaction window.

## Features

- Animated predicate scan window and range markers
- Phantom insert blocker with conflict pulses
- Responsive transaction dashboard
- Accessible semantic demo markup