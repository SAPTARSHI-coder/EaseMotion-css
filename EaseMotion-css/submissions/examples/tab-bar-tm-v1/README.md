# Animated tab bar

1. What does this do? A horizontal tab list with a sliding indicator that follows the active tab.

2. How is it used? Add `tab-bar-tm` markup to your page — see demo.html for the class names.

3. Why is it useful? Familiar nav pattern; the indicator animates between tabs without a layout shift.
